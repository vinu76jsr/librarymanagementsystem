language: python
python:
  - "2.7"
# command to install dependencies
install:
  - "pip install -r requirements.txt"
# command to run tests
script:
  - python manage.py test mainapp
env:
  - COVERALLS_REPO_TOKEN=abcdef1234569abdcef
 
after_success:
  - coveralls
